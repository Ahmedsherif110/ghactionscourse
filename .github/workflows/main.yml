name: first workflow

on:
  push:
    branches:
      - main 

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps: 
      - name: print welcome message
        run: echo "Welcom Rkha!"

      - name: checkout repository  
        uses: actions/checkout@v6.0.1

      - name: list repositry files
        run: ls -la
        
